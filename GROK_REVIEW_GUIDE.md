# ES 源码精读站：交给 Grok 的优化计划

审查日期：2026-09-25。范围是 `index.html`、30 个 `chapters/chNN.html`、414 个实验注册项、22 个动画、控制台、速查表和 Docker 配置。`tutorial/` 是选题与例子的参考，不是事实权威；网页以实际使用的 Elasticsearch 9.4.0 镜像、对应 tag 的源码和可复现 API 输出为准。本次是静态审查加少量只读 API 核对，**没有逐一执行 414 个实验，也没有完成浏览器视觉/交互验收**。

## 总判断

这个站点可继续优化：30 课已齐，主线顺序合理，图与“预测→操作→对照”很有用，`node tools/check.mjs` 和 `node tools/figwidth.mjs` 均通过。但结构通过不等于事实正确或实验能完成。先修版本锚点和控制台执行路径，再补源码阅读与分布式实验的证据，最后做逐章体验验收。保留现有讲解中已经成立的机制与图，不必推倒重写。

## P0：已定位的错误与阻断

| 位置 | 证据/影响 | 修改要求与验收 |
|---|---|---|
| `chapters/ch01.html`、`ch02.html`、`ch20.html`、`experiments/ch02.js`；上游 `tutorial/README.md` 等 | 网页多处称 ES 9.4.0 对应 Lucene **10.3.2**；本地教程容器 `GET /` 返回 `version.number=9.4.0`、`version.lucene_version=10.4.0`。第 2 课实验的预期输出因此必错。`GET /_nodes?filter_path=nodes.*.jvm.version` 返回 `26.0.1`，而第 2 课与实验预期写 `25.0.2+10`。官方仓库 [v9.4.0/version.properties](https://github.com/elastic/elasticsearch/blob/v9.4.0/build-tools-internal/version.properties)进一步确认 Lucene **10.4.0**、bundled JDK **26.0.1+8**、Netty **4.1.132.Final**；第 2 课版本表的 Netty **4.1.130.Final** 也错。 | 按固定 v9.4.0 tag 修全站相关数字、代码注释和实验预期；把 tutorial 的旧数字登记为参考资料勘误。其他库版本也逐项核对，避免只改三处显眼数字。验收：第 2 课版本表和两项实验预期与 tag 源码、真实响应一致。 |
| `assets/js/console.js` 的 `currentRequest()`；`chapters/ch13.html` | 控制台对 body 执行 `.trim()`，删掉 Bulk NDJSON 必需的末尾 `\n`。第 13 课的正常 Bulk 从“在控制台中打开”进入后会变成请求级 400。Elastic 的 [Bulk API 文档](https://www.elastic.co/docs/api/doc/elasticsearch/operation/operation-bulk)也明确要求最后一行以换行结束。 | 保留原始 body 的末尾换行；JSON 校验可以用临时副本，但发送原文。用第 13 课 `ch13-01-mixed` 和 `ch13-04-malformed` 分别验收“合法末尾换行→200”和“故意缺换行→400”。 |
| `assets/js/console.js` 的请求体验证；`experiments/ch13.js` | 章节运行器支持 `allowInvalidBody`，第 13 课实验 2 的坏 source 行本应送给 ES 观察“HTTP 200 + 条级失败”；控制台会提前显示“JSON 无效”，用户从深链无法复现实验。 | 控制台允许明确标记的破坏实验原样发送，或提供“仍然发送原文”入口；只对用户输入给提示，不替 ES 决定响应。验收：章节按钮和控制台深链出现相同的 HTTP 状态与 `items[]`。 |
| `assets/js/experiments.js` 的 `consoleLink`；`assets/js/console.js` | 深链只带 method/path/body，不带实验目标端点与认证上下文。第 29 课实验固定发 9201 并分别用匿名、elastic、alice 凭据；点“在控制台中打开”后默认回到 9200/全局凭据，结果会变。 | 给深链增加安全、显式的端点选择和认证模式，或在跳转前清楚提示用户设置目标端点和凭据；不要把明文密码放 URL。验收：第 29 课匿名 401、elastic 200、alice 403 的章节按钮与控制台可复现。 |
| `chapters/ch13.html:99`、`assets/js/es-client.js` | 正文说“本站实验运行器把 body 以 `application/json` 发出”；实际代码把非整体 JSON 的 NDJSON 发为 `application/x-ndjson`。 | 改正文，保留对两种 Content-Type 均可接受的说明；验收时用浏览器 Network 面板或本地请求记录确认。 |
| `cheatsheet.html:33` | `GET /` 被描述成可看“角色”，但当前根响应只有节点名、集群名、版本等，没有 `roles`；角色要看 `_nodes` 或 `_cat/nodes`。 | 改为“版本、集群名、节点名”；角色另指向正确 API。 |
| `docker/docker-compose*.yml`、`assets/js/es-client.js`、`experiments/ch29.js` | 9200/9201/9300 使用默认主机端口映射，HTTP CORS 放行 `*`，第 29 课的管理员口令硬编码，控制台又把 Basic 凭据存入 `localStorage`。这适合封闭学习机的演示，但若机器在局域网或浏览器打开外部网页，暴露面明显增大。[Elastic 网络配置文档](https://www.elastic.co/docs/reference/elasticsearch/configuration-reference/networking-settings)也将 CORS `*` 视为安全风险。 | 本地学习配置将端口显式绑定 `127.0.0.1`；检查 `file://` 下的 CORS 需求后收窄允许来源，至少在站点入口写明仅限本机实验；第 29 课凭据不要默认持久化，提供清除入口。验收：本机站点可运行、其他网卡不可连、关闭页面后凭据策略符合说明。 |

## P1：讲解不足与需要核实的说法

1. **恢复“源码精读”的动手闭环。** 第 2–4 课明确把原教程的仓库实验改为思考题，只保留 2 个 HTTP 观察；读者可能读完却没有打开过 `RestGetAction`、`Node.start()` 或一个插件描述文件。给第 2 课加入“取得 ES 9.4.0 tag → 定位类/方法 → 回答一个具体分支”的最短准备流程；第 3、4 课各加一个真正打开源码并用测试或 API 对照的任务。后续章节中的源码路径有缩写，建议关键路径写完整并链接到**固定 tag**，同时标注“文件→方法→要验证的断言”。不要仅增加类名表。
2. **第 9、22–24 课的核心机制缺少可运行观察。** 当前单节点只能看本地传输统计；选举/多数派不能制造真实分区，Peer Recovery 没有第二节点，快照实验只证明 `path.repo` 未配置会报错。优先提供可选的三节点或双节点 Docker 场景，以及独立的 fs 仓库场景，写明资源需求、启动/恢复/清理命令和成功输出；若不加环境，就把这些部分清楚标为“源码推演”，不要称为已实测的分布式实验。第 24 课至少应有一次**成功创建并恢复快照**的可选路径。
3. **第 29 课字段级/文档级权限只在正文一段带过。** tutorial 将 FLS/DLS 列为专题目标；网页目前主要演示 401/403 和 RBAC。补一组“同一用户能搜同一索引，但看不到字段/文档”的对照，或缩小章节目标并说明该高级部分仅作入口。注意授权实验的准备/清理和版本许可条件。
4. **第 1、13、14 课的默认 refresh 说法过于绝对。** “默认每 1 秒刷新”“最多晚约 1 秒”应加条件：未显式设置 `index.refresh_interval` 时，长时间没有搜索的 shard 会进入 search-idle，不持续后台 refresh；下一次搜索可能触发 refresh。官方[索引设置说明](https://www.elastic.co/docs/reference/elasticsearch/index-settings/index-modules)有该条件。当前第 1 课用 `refresh_interval=-1` 固定实验窗口，实验本身可保留；修概念句、速记表和速查表即可。
5. **第 1 课分析器例子缺少边界。** `ch01.html:121` 直接说“送进分析器”后会去掉停用词、写入查询走同一链；本页稍后才说明内置 `standard` 默认不去停用词。先在图前声明这是显式 `standard tokenizer + lowercase + stop` 的示例；说明字段可指定 `search_analyzer`，两边不保证配置完全相同，只需产生可匹配的 token。第 1 课的 `GET /`、分词、近实时三个概念应避免用绝对性能数字（“十亿条日志几十分钟”“ES 毫秒级”）代替可复现条件。
6. **实验数量与阅读时间不匹配。** 30 课共 414 个实验，原网页方案写每课 2–4 个，现实中第 14 课 29 个、第 12/24/26/28 课各 20 个。很多是前置、观察、清理的一条流水线。请按“核心任务 2–4 组，每组有连续步骤”重排显示，标注预计耗时、前置状态、可否重跑和一键/逐步清理；保留必要子步骤与错误分支，不为了数字机械删实验。第 28 课还会改**集群全局** ILM poll 设置，特别要保证失败中途可恢复。
7. **速查表覆盖范围太窄。** 当前只覆盖 `_cat`、线程池、文档/搜索和 refresh/flush/translog，与六篇课程范围不符。补“请求路径/Action 名”“Bulk 状态判读”“搜索两阶段与深分页”“集群状态/选举”“恢复与快照”“ILM/安全”六组最小索引，每项链接到具体章节和一个可运行示例。速查表中的版本数字要由 P0 的统一锚点驱动。

## P2：全站验收与交付约束

- **先写可追踪勘误。** 每一处事实改动记录“网页原句 → 证据（9.4.0 API/固定 tag 源码/官方文档）→ 修改后说法 → 验收结果”。`tutorial/` 仅作对比；若它也错，登记，不把错文案回灌到网页。
- **补有意义的检查。** `tools/check.mjs` 目前只看结构、注册表与引用，无法发现版本错、NDJSON 被截断、目标端点丢失或实验预期不符。加少量针对这些回归点的自动检查；不要把正文每句话硬编码成快照测试。
- **逐章抽验，再做完整流水线。** 每篇选至少一课从干净索引按顺序跑到清理；第 13、24、28、29 课必须完整走。记录 ES 版本、HTTP 状态、关键字段、是否可重跑，以及对全局设置的残留。跑实验前用专用索引名并确认清理范围。
- **浏览器验收。** 用 `file://` 和本地 HTTP 各开一次，在窄屏/宽屏、深色/浅色下检查图、动画、折叠答案、控制台深链、连接状态与键盘操作。`figwidth` 只是估算 SVG 文本宽度，不能代替浏览器排版。
- **完成标准。** P0 全部修复；主要源码任务和分布式/快照的可选路径能独立完成；第 13/24/28/29 课的关键实验有真实响应证据；结构检查与浏览器验收通过；`STATUS.md` 增加本轮勘误和验收记录。提交给用户时列出仍无法验证的论断，不写“全站无误”。

建议 Grok 按 P0 → P1 → P2 分批提交，每批附修改文件、证据、实际运行结果与剩余问题。第一批先处理版本数字、Bulk 控制台路径和第 29 课深链，这三项会让读者直接得到错误答案。
